name: PropCount

# Сайт в Docker. API — отдельный проект на https://api.propcount.ru
services:
  app:
    image: ghcr.io/koj10/arenda:${TAG:-latest}
    ports:
      - "3005:80"
    restart: unless-stopped
