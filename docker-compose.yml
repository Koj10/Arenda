name: PropCount

# Прод на сервере: контейнер на :3001, снаружи HTTPS делает хостовый nginx.
services:
  app:
    image: ghcr.io/koj10/arenda:${TAG:-latest}
    ports:
      - "3001:80"
    restart: unless-stopped
