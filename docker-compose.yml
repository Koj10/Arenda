name: PropCount

# Сайт в Docker на :3005. :3001 на сервере занят API (uvicorn).
# Снаружи HTTPS — хостовый nginx (deploy/host-nginx-proxy.conf).
services:
  app:
    image: ghcr.io/koj10/arenda:${TAG:-latest}
    ports:
      - "3005:80"
    restart: unless-stopped
